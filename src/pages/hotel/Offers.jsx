import { Box } from "@mui/material";

const Offers = () => {
  return (
    <Box sx={{ width: '100%', boxShadow: 'inset 0px 0px 15px rgba(0, 0, 0, 0.15)' }}>
      <h3>Offers</h3>
    </Box>
  )
};

export default Offers;
